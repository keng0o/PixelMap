# 連続表示方位を論理ピクセルグリッドへ再ラスタライズする

PixelMapの本番体験は、90度単位の回転ではなく、自動更新される連続的な表示方位を採用する。追従方位には、停止・低速時の端末方位と、移動時の移動方位を滑らかに切り替えるハイブリッド方式を使い、standalone testだけは立体とUIの評価用に連続方位を手動変更できる。完成した出力画像を回転せず、方位変換後の地物を論理ピクセルグリッドへ再ラスタライズし、正立要素を分離して描画することで、自然な地図体験と鮮明なピクセル表現を両立する。
