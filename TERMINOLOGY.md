# PixelMap 用語集

PixelMap では、座標系や役割の異なる単位を同じ「ピクセル」「グリッド」「タイル」と呼ばない。単位を書くときは、原則として次の修飾語を付ける。

## 描画の単位

| 用語 | 定義 | 現在の値 |
| --- | --- | --- |
| 論理ピクセル（logical pixel） | 最終的な地図画像を構成する最小単位。アセットの1ドットもこの単位で設計する。 | 出力画像の1px |
| シーンピクセル（scene pixel） | オフスクリーンの作業用キャンバス上の1px。位置や線幅など、レンダラー内部の座標単位。 | 2×2シーンpx = 1論理px |
| CSSピクセル（CSS pixel） | ブラウザー上で表示サイズを決めるCSSの `px`。論理ピクセルとの比率は表示方法に依存する。 | 単独テストページでは1論理px = 表示倍率ぶんのCSS px |
| デバイスピクセル（device pixel） | ディスプレイの物理画素。ブラウザーとOSのdevice pixel ratioに依存する。 | 実装の基準にはしない |

「1px」は単独で使わず、「1論理px」「2シーンpx」「1 CSS px」のように座標系を明記する。

## 地図の単位

| 用語 | 定義 | 現在の値 |
| --- | --- | --- |
| マップセル（map cell） | 土地・道路・建物などの種別を1つ保持する、地図ラスタライズの正方形単位。「マス」は説明文でのみ使い、コード上の概念名はセルとする。 | 16×16シーンpx = 8×8論理px |
| マップグリッド（map grid） | マップセルを行列状に並べたデータ。地物をラスタライズし、オートタイリングへ渡す。 | MVTタイル1枚につき96×96セル。描画時は外周を加えた98×98セル |
| 論理ピクセルグリッド（logical-pixel grid） | 全アセットの輪郭・線幅・配置を論理ピクセル境界へそろえる規則。マップグリッドとは別物。 | 768×768論理px |

「グリッド」だけではマップグリッドを指す。ピクセル境界の話では、必ず「論理ピクセルグリッド」と書く。

## タイルとアセット

| 用語 | 定義 | 現在の値 |
| --- | --- | --- |
| MVTタイル（MVT tile） | OpenFreeMapから取得する地理データの単位。ズーム、X、Yで識別する。 | z14、1枚 = 1536×1536シーンpx |
| マップチップ（map chip） | 1マップセルへ描く地面・道路・屋根などの小画像。MVTタイルとの混同を避けるため、描画素材を「タイル」と呼ばない。 | 16×16シーンpx = 8×8論理px |
| スプライト（sprite） | 施設アイコンなど、複数の論理ピクセルからなる独立した描画アセット。 | 素材座標の1単位 = 1論理px |
| アセット（asset） | マップチップ、スプライト、文字画像など、再利用する描画素材の総称。 | 輪郭は論理ピクセルグリッドへそろえる |

## 画面と倍率

| 用語 | 定義 | 現在の値 |
| --- | --- | --- |
| シーン（scene） | 地図を描き込む作業用キャンバス。MVTタイル1枚と同じ範囲を持つ。 | 1536×1536シーンpx |
| 出力画像（presentation） | シーンを最近傍法で縮小した、利用者へ渡すキャンバス画像。 | 768×768論理px |
| 表示倍率（display scale） | 単独テストページで、出力画像をCSS上何倍に表示するかを表す整数。地図の解像度やアセットのドット密度は変えない。埋め込みページは表示枠へレスポンシブに合わせるため、この値では表さない。 | 通常1。必要な場合のみ整数倍 |

換算関係は次のとおり。

```text
1 MVTタイル
= 96 × 96 マップセル
= 1536 × 1536 シーンピクセル
= 768 × 768 論理ピクセル

1 マップセル
= 16 × 16 シーンピクセル
= 8 × 8 論理ピクセル

1 論理ピクセル
= 2 × 2 シーンピクセル
= 単独テストページでは 表示倍率 × 表示倍率 の CSSピクセル
```

## 命名ルール

- `scene` は1536pxの作業座標、`presentation` は768pxの出力画像に使う。
- `logicalPixel`、`scenePixel`、`cssPixel` のどれかを明示し、裸の `pixel` は新しい名前に使わない。
- `cell` はマップセル、`grid` はマップグリッドに使う。CSS GridはCSS文脈に限る。
- `tile` はMVTタイルに使う。描画素材は `mapChip`、施設素材は `sprite` と呼ぶ。
- `scale` には対象を付ける。表示倍率は `displayScale`、座標変換は `scenePixelsPerLogicalPixel` のように書く。

## 2×2単色セル検証

単独ページの `?render=cell2` は、実験用マップセルを「2×2論理px（4×4シーンpx）の不可分な単色ブロック」として扱う描画モードである。完成画像を粗く縮小する方式ではなく、MVT地物を384×384マップグリッドへラスタライズし、意味情報から地形・交通・建物・施設・地形記号を単色セルとして合成する。標準描画のマップセルは引き続き8×8論理pxである。

このモードでも名称と現在地表示は読みやすさを保つため論理ピクセル単位で重ねる。埋め込みページでは `render=cell2` を無視し、旧`render=cell8`を含む未知の値も通常描画へフォールバックする。
