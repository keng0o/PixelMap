# 連続表示方位を論理ピクセルグリッドへ再ラスタライズする

PixelMapの表示方位は、90度単位に限定せず連続値として扱う。Web本番とstandalone testではコンパスの−／＋から15度ずつ手動変更でき、複数地図では中心地点を変えずに同期表示方位を共有する。将来の現在地追従では、停止・低速時の端末方位と移動時の移動方位を滑らかに切り替えるハイブリッド方式を採用する。完成した出力画像を回転せず、方位変換後の地物を論理ピクセルグリッドへ再ラスタライズし、正立要素を分離して描画することで、自然な地図体験と鮮明なピクセル表現を両立する。
